include_directories(.)

project(stack)
add_executable(stack src/stack.cpp)

project(queue)
add_executable(queue src/queue.cpp)

project(linklist)
add_executable(linklist src/linklist.cpp)

project(modernCpp)
add_executable(modernCpp src/ModernCpp.cpp)